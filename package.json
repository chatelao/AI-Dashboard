{
  "name": "ai-dashboard-root",
  "private": true,
  "version": "1.0.0",
  "description": "Root package for Playwright tests",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "playwright": "^1.59.1"
  }
}
